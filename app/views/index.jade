extends layout

block content
  h1= title
  p Welcome to #{title}
  script(src='socket.io/socket.io.js')
  script.
      var init = function(){
      socket = io.connect();
      }
      window.onload = init;

